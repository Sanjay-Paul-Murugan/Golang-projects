# Simple Go Web Server

A basic web server built with Golang that serves static HTML files and handles form submissions.

## Project Structure
- `static/`: Contains the frontend HTML files.
- `main.go`: The core server logic.

## How to Run
1. Ensure you have Go installed.
2. Run the command:
   ```bash
   go run main.go